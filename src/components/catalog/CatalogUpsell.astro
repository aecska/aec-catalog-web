<section class="upsell-section fade-up">
  <div class="upsell-card">
    <div class="text-side">
      <div class="upsell-badge">OPCIÓN RECOMENDADA</div>
      <h2>¿QUIERES ESCALAR TU NIVEL?</h2>
      <p>No compres uno por uno. Adquiere colecciones completas por categoría o llévatelo todo con nuestros Bundles Maestros AEC.</p>
    </div>
    <a href="/bundles" class="btn-upsell">
      Ver Catálogo de Bundles
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <line x1="5" y1="12" x2="19" y2="12"></line>
        <polyline points="12 5 19 12 12 19"></polyline>
      </svg>
    </a>
  </div>
</section>

<style>
  .upsell-section { 
    margin: 60px auto 100px; 
    max-width: 1100px; 
    width: calc(100% - 40px); 
  }

  .upsell-card { 
    background: var(--green); 
    color: #000; 
    border-radius: 12px;
    padding: 40px 48px; 
    display: flex; 
    align-items: center; 
    justify-content: space-between; 
    gap: 40px; 
    position: relative; 
    overflow: hidden;
    border: none;
    text-align: left;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  /* Decorative Noise/Glow */
  .upsell-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
    opacity: 0.08;
    pointer-events: none;
  }

  .upsell-card .text-side { flex: 1; position: relative; z-index: 2; }

  .upsell-badge {
    display: inline-block; 
    background: rgba(0, 0, 0, 0.08); 
    padding: 4px 10px;
    border-radius: 2px; 
    font-family: var(--font-mono); 
    font-size: 9px; 
    font-weight: 800;
    margin-bottom: 12px; 
    color: #000;
  }

  .upsell-card h2 { 
    font-family: var(--font-display); 
    font-size: 44px; 
    line-height: 0.9; 
    margin-bottom: 8px; 
    color: #000; 
    text-transform: uppercase;
  }

  .upsell-card p { 
    font-size: 15px; 
    margin: 0; 
    opacity: 0.9; 
    font-weight: 600; 
    line-height: 1.4; 
  }
  
  .btn-upsell {
    position: relative;
    z-index: 2;
    display: inline-flex; 
    align-items: center; 
    gap: 12px; 
    background: #000; 
    color: var(--white);
    padding: 16px 32px; 
    border-radius: 4px; 
    font-weight: 800; 
    font-size: 14px; 
    text-decoration: none;
    transition: all 0.3s; 
    white-space: nowrap;
  }

  .btn-upsell:hover { 
    transform: translateY(-3px); 
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  }

  @media (max-width: 900px) {
    .upsell-card {
      flex-direction: column;
      padding: 40px;
      text-align: center;
      gap: 30px;
    }

    .btn-upsell {
      width: 100%;
      justify-content: center;
    }
  }
</style>
