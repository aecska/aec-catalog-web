<section id="bundles-upsell" class="fade-up">
  <div class="upsell-card">
    <div class="text-side">
      <h2>¿BUSCAS ALGO MÁS ESPECÍFICO?</h2>
      <p>Si no necesitas la colección completa, puedes explorar nuestra biblioteca de Mega Packs individuales para llevar solo lo que necesitas.</p>
    </div>
    <a href="/megapacks" class="btn-black">
      Ver Packs Individuales
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="5" y1="12" x2="19" y2="12"></line>
        <polyline points="12 5 19 12 12 19"></polyline>
      </svg>
    </a>
  </div>
</section>

<style>
  #bundles-upsell {
    margin: 60px auto 100px;
    max-width: 1100px;
    width: calc(100% - 40px);
  }

  .upsell-card {
    background: var(--green);
    color: #000;
    border-radius: 12px;
    padding: 48px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
    position: relative;
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  /* Decorative Noise Overlay */
  .upsell-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
    opacity: 0.1;
    pointer-events: none;
  }

  .text-side {
    position: relative;
    z-index: 2;
    flex: 1;
    text-align: left;
  }

  h2 {
    font-family: var(--font-display);
    font-size: 48px;
    line-height: 0.9;
    margin-bottom: 12px;
    color: #000;
    text-transform: uppercase;
  }

  p {
    font-size: 15px;
    margin: 0;
    opacity: 0.95;
    font-weight: 600;
    line-height: 1.4;
    max-width: 500px;
  }

  .btn-black {
    position: relative;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    gap: 12px;
    background: #000;
    color: var(--white);
    padding: 18px 36px;
    border-radius: 4px;
    font-weight: 800;
    font-size: 14px;
    text-decoration: none;
    transition: all 0.3s;
    white-space: nowrap;
    text-transform: uppercase;
  }

  .btn-black:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
  }

  @media (max-width: 900px) {
    .upsell-card {
      flex-direction: column;
      text-align: center;
      padding: 40px 24px;
      gap: 30px;
    }

    .text-side {
      text-align: center;
    }

    h2 {
      font-size: 36px;
    }

    .btn-black {
      width: 100%;
      justify-content: center;
    }
  }
</style>
